[2026-05-14T20:40:00-04:00] CODEX: TODO: VERIFY built-in preset key `com.apple.universalaccess reduceMotion` against Apple documentation or a macos-defaults.com entry for the exact defaults key.
[2026-05-14T20:40:00-04:00] CODEX: TODO: VERIFY built-in preset key `com.apple.universalaccess reduceTransparency` against Apple documentation or a macos-defaults.com entry for the exact defaults key.
[2026-05-14T20:40:00-04:00] CODEX: TODO: VERIFY built-in preset key `NSGlobalDomain AppleInterfaceStyle` against Apple documentation or a macos-defaults.com entry for the exact defaults key.
[2026-05-14T20:40:00-04:00] CODEX: TODO: VERIFY built-in preset key `NSGlobalDomain NSAutomaticWindowAnimationsEnabled` against Apple documentation or a macos-defaults.com entry for the exact defaults key.
[2026-05-14T20:40:00-04:00] CODEX: TODO: VERIFY built-in preset key `com.apple.dock expose-animation-duration` against Apple documentation or a macos-defaults.com entry for the exact defaults key.
[2026-05-14T20:42:58-04:00] CODEX: `xcodebuild test -scheme Aura -project Aura.xcodeproj -destination 'platform=macOS'` fails because scheme `Aura` is not configured for the test action, so XCTest cannot be executed from the current project setup.
[2026-05-14T20:42:58-04:00] CODEX: Project builds are also blocked by duplicate resource outputs from file-system-synchronized `.gitkeep` and `README.md` files across `Aura/Core`, `Aura/UI`, `Aura/Shared`, and `Aura/Resources`.
[2026-05-14T21:05:00-04:00] CLAUDE_CODE: TODO: VERIFY macOS 26 Liquid Glass API — LivePreviewView.swift and PresetGalleryView.swift use `.background(.ultraThinMaterial)` for the mock menu bar, dock, and card surfaces. If `.glassEffect()` or another confirmed macOS 26 API is available, update those sites (marked with inline TODO comments).
[2026-05-14T21:18:15-04:00] CLAUDE_CODE: Requested build destination `platform=macOS,arch=arm64` is unavailable in this environment. Available destinations are `platform=macOS,arch=x86_64,name=My Mac` and `platform=macOS,name=Any Mac`. Validation must run against one of those destinations here.
[2026-05-14T21:19:39-04:00] CLAUDE_CODE: `xcodebuild test -project Aura.xcodeproj -scheme Aura -destination 'platform=macOS,arch=x86_64'` fails because scheme `Aura` is not configured for the test action. Manual Xcode steps needed: 1) open `Aura.xcodeproj`, 2) add or confirm a macOS unit test target and attach `AuraTests/CoreTests`, 3) Product > Scheme > Edit Scheme, 4) select the `Test` action and include the test target, 5) save the shared scheme, 6) rerun `xcodebuild test`.
